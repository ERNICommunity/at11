trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '10.16.x'
  displayName: 'Install Node.js'

- script: |
    npm install
    npm run build
  displayName: 'install and build'

- script: npm test
  displayName: 'run tests'